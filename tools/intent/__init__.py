"""Intent tree tooling for the ICE methodology.

Deliberately dependency-free: the tree is the harness, so its checker must run
anywhere Python runs, without an install step.
"""

__version__ = "2.0.0"
